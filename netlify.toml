[build]
# Base directory for the build (website app in monorepo)
base = "apps/website"
# Build command
command = "pnpm run build"
# Publish from the Next.js static export directory (relative to base)
publish = "out"

[build.environment]
# Specify Node.js version
NODE_VERSION = "22"
